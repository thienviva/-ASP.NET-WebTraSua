@model IEnumerable<WebQuanLyQuanTraSua.Models.PHANCA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/AdminLayout.cshtml";
}

<h2>Phân Ca Cho Nhân Viên</h2>

<p>
    <div class="btn btn-default">
        @Html.ActionLink("Thêm mới", "Create")
        </div>
</p>
<table class="table">
    <tr>
        <th>
            Ngày làm việc
        </th>
        <th>
            Số giờ làm việc
        </th>
        <th>
            Tên chi nhánh
        </th>
        <th>
            Tên nhân viên
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NgayLamViec)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GioLamViec)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CHINHANH.TenCN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NHANVIEN.TenNV)
        </td>
        <td>
            @Html.ActionLink("Chỉnh sửa", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Chi tiết", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Xóa", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
